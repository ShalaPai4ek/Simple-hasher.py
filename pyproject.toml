[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "simple-hasher"
version = "0.1.0"
description = "Simple hasher on python"
requires-python = ">=3.8"
dependencies = []

[project.scripts]
simple-hasher = "simple_hasher:main"
